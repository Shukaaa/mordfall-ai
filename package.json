{
  "name": "mordfall-ai",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd backend && bun install && cd ../frontend && npm install",
    "start:all": "concurrently \"cd backend && npm run start\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && bun run build && cd .. && node scripts/copy-builds.js && cd build && bun install"
  },
  "author": "Shukaaa",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
